## Ejercicio 2.1 (Extensión del Singleton)

- Se agregó un método `reset()` a `singleton.py` y se créo un test el para dicho método.

- Probar test desde la carpeta `LaboratorioModificado`:

    ```sh
    python3 -m tests.test_singleton_reset
    ```

- Se agregó archivos `salida_terraform.txt` y `salida_test.txt`