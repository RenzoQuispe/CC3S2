[tool:pytest]
addopts = -v --tb=short --cov=models --cov-report=term-missing
filterwarnings = ignore::DeprecationWarning

[coverage:run]
branch = True
omit =
    */tests/*
    */test_*

[coverage:report]
show_missing = True
